# Solve - r/place (2/2)

Je ferai un solve plus complet avec le code plus tard, mais en gros voici l'idée :  
Ici on est limité par le cooldown de 10 secondes, mais ce cooldown est appliqué par compte.  
Il faut donc, en utilisant l'API mise à disposition en analysant le code :  
- Générer plein de comptes  
- Se connecter à ces comptes pour récupérer les tokens de connexion  
- Dessiner sur le canvas à l'aide de tous les tokens de connexion pour ne plus être limité par le cooldown  
- Win-Win
